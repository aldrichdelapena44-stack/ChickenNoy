/*
  Chickenoy deployment config
  ---------------------------
  For Vercel frontend + Render backend:
  1. Deploy the Backend folder to Render first.
  2. Copy the Render URL, for example https://chickenoy-backend.onrender.com
  3. Paste it below as API_BASE.

  If you deploy the full app only on Render, you can leave API_BASE empty.
*/
window.CHICKENOY_CONFIG = {
  API_BASE: ""
};
